#include <cassert>

#include <grid.h>
#include <param.h>

void Grid::build_grid (const Param *){
    assert (false);
}

void Grid::export_grid (const Param *){
    assert (false);
}

void Grid::import_grid (const Param *){
    assert (false);
}

//END
