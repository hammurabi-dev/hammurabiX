#include <cassert>

#include <tinyxml2.h>

#include <grid.h>

void Grid::build_grid (tinyxml2::XMLDocument *){
    assert (false);
}

void Grid::export_grid (){
    assert (false);
}

void Grid::import_grid (){
    assert (false);
}

//END
